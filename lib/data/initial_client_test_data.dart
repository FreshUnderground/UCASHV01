/// Classe pour la gestion des données clients initiales
/// En production, cette classe est désactivée
class InitialClientTestData {
  /// Méthode désactivée en production
  /// Les clients doivent être créés via l'interface administrateur
  static Future<void> createTestClients() async {
    // Désactivé en production pour des raisons de sécurité
    // Les clients doivent être créés manuellement par les administrateurs
    return;
  }
}
